//'use strict';

/*const htmlElement = document.querySelector('.message').textContent;
console.log(htmlElement);

document.querySelector('.number').textContent = 13;

document.querySelector('.message').textContent = '🎉 Correct Number ';

document.querySelector('.score').textContent = 10;

document.querySelector('.guess').value = 23;



//variable  to use addeventlistener
const highScoreColor = document.querySelector('.label-highscore');

//to choose color on background color

const bodyColor = document.querySelector('body');

//call the variable where it will be clicked then the addEventListener then input the action to be listen ex. 'click' and then function.

highScoreColor.addEventListener('click', function () {
  bodyColor.classList.add('new');
});
*/
///////////////////////////////////////////////////////////

/* 

document.querySelector('body').classlist.add('')



JavaScript in the Browser: DOM and Events
Coding Challenge #1
Implement a game rest functionality, so that the player can make a new guess!
Your tasks:
1. Select the element with the 'again' class and attach a click event handler
2. In the handler function, restore initial values of the 'score' and
'secretNumber' variables
3. Restore the initial conditions of the message, number, score and guess input
fields
4. Also restore the original background color (#222) and number width (15rem)
GOOD LUCK �
*/
